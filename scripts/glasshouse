#!/bin/sh
set -eu

if ! command -v uvx >/dev/null 2>&1; then
  echo "glasshouse skill: uv is required; install it from https://docs.astral.sh/uv/" >&2
  exit 127
fi

exec uvx --from git+https://github.com/Provn-Inc/glasshouse.git glasshouse "$@"
